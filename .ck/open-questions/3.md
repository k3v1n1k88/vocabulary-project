# Open questions for #3

Raised by `/ck:plan validate` during routine bootstrap on 2026-05-17.
A human reviewer should resolve these before the PR is marked
ready-for-review.

## Questions

1. Should the new Gemini default model be `gemini-2.5-flash-preview-05-20` (latest, best quality but a preview — could deprecate faster) or `gemini-1.5-flash` (stable GA — less likely to be deprecated again)? The brainstorm recommends the preview but flags the risk.

2. Is adding OpenRouter, Groq, and Mistral (Phase 3) in scope for this issue, or should it be a follow-up? The README already lists all 6 providers, suggesting they were always intended — but the issue body only mentions Gemini and a general "check other providers" ask.

3. For Grok, what is the correct stable model alias to use? The plan proposes `grok-2-latest` as default and `grok-2-1212` as the pinned option. Please confirm against the current xAI API docs before Phase 1 ships.

4. `llmModel` is declared in `UserSettings` (types/index.ts:87) but is absent from `defaultSettings` in `store.ts`. Should it be added to `defaultSettings` (e.g., `llmModel: undefined`) for clarity, or is the current omission intentional (relies on optional fallback to `config.defaultModel`)?
