# Open questions for #5

Raised by `/ck:plan validate` during routine bootstrap on 2026-05-17.
A human reviewer should resolve these before the PR is marked
ready-for-review.

## Questions

1. Should the pre-existing bug where `settings.llmModel` is saved in `UserSettings` but never read by `translation-service.ts` (which always uses `config.defaultModel`) be fixed in this issue, or tracked as a separate issue? The current plan intentionally leaves it out of scope.

2. Should `gemini-2.0-flash-lite` (still active as of May 2026) be included as a third Gemini option alongside `gemini-2.5-flash` and `gemini-2.5-pro`, or excluded to keep the list 2.5-series only?

3. The `grok-4.3` model ID was sourced from a third-party aggregator, not the official xAI API docs (`docs.x.ai/developers/models`). Confirm this ID is correct before implementation — if wrong, the Grok default should fall back to `grok-3-mini` only until verified.
